server:
  port: 8091

spring:
  application:
    name: dashboard-service

  datasource:
    url: jdbc:mysql://localhost:3317/dashboard_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
